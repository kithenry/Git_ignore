* Description
  A script to speed up .gitingore file creation across projects


* Features supported
  - Creates .gitignore file in folder where its called if it does not exits yet.
  - Updates .gitignore file with new extensions of file patterns
  to ignore

* Features still under creation
  - Can be installed as a package
  - Creates .gitignore across all folders in project
  - Reading .ignore items from externally provided txt file


* Installation
  - Clone this repo and cd into cloned directory
  - run
  #+begin_src sh
    pip install dist/git_ignore-0.0.1-py3-none-any.whl
  #+end_src 
